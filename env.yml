name: cyclegan_tst
channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - pip
  - python
  # - cudatoolkit=11.3
  - pytorch
  - pytorch-mutex=1.0=cuda
  - scikit-learn
  - tokenizers
  - transformers
  - pip:
      - bert_score
      - rouge_score
      - sacrebleu
      - evaluate
      - numpy
      - pandas
      - comet_ml
      - dotenv
